#include <stdio.h>
#include <stdlib.h>

int main() {
    int n, sum = 0;
    
    // Read the size of the array
    if (scanf("%d", &n) != 1) return 1;

    // Dynamically allocate memory for n integers
    int *arr = (int*)malloc(n * sizeof(int));
    
    // Read n integers into the array and calculate sum
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
        sum += arr[i];
    }

    // Print the final sum
    printf("%d\n", sum);

    // Free the allocated memory
    free(arr);
    
    return 0;
}
